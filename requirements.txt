# ── LLM & Agent Orchestration ──
langgraph>=0.2.0
langchain>=0.2.0
langchain-community>=0.2.0  # includes ChatOllama + OllamaEmbeddings + Replicate
langchain-aws>=0.1.0
langchain-openai>=0.1.0     # openai fallback / embeddings

# ── LLM Providers ────────────────────────────────────────────────────────────
# Ollama  → install separately from https://ollama.ai, then: ollama pull mistral
# Replicate → free tier cloud API for prompt experimentation
replicate>=0.25.0
# SageMaker → via boto3 (see AWS section below)

# ── Observability ──
langfuse>=4.0.0,<5.0.0

# ── Vector Store / RAG ──
faiss-cpu>=1.7.4

# ── Classical ML ──
scikit-learn>=1.4.0
# CPU-only xgboost — GPU version pulls nvidia/CUDA dependencies (running on EC2)
xgboost-cpu>=2.0.0
numpy>=1.26.0
pandas>=2.2.0
joblib>=1.3.0

# ── Document Parsing ──
pypdf>=4.0.0
python-docx>=1.1.0
pdfminer.six>=20221105

# ── API & Config ──
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9

# ── AWS (SageMaker + Bedrock + S3) ──
boto3>=1.34.0
botocore>=1.34.0
#sagemaker>=2.224.0  # install manually when deploying: pip install sagemaker

# ── Eval ── no implemented now
#dvc>=3.50.0
#dvc-s3>=3.2.0

# ── Dev / Test ──
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
python-dotenv>=1.0.0
